# dependencies (none yet, but keep for future)
node_modules/

# stray lockfiles — this package has zero deps and uses npm
pnpm-lock.yaml
yarn.lock
package-lock.json

# local scan cache — never commit user data
.cache/

# logs
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# editors
.vscode/
.idea/

# build / coverage
coverage/
*.lcov

# env
.env
.env.*
